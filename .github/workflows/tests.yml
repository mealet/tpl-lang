name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  tests:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4 
    
    - name: 📣 Test Lexer
      run: cargo test —-package tpl_lexer 

    - name: 📣 Test Parser
      run: cargo test —-package tpl_parser 

      # your requires LLVM and separated functions for tests
